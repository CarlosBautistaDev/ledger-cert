# =============================================================================
# Ledger de Certificados — Backend dependencies (pinned).
# Mismo stack que el proyecto de referencia: Django 5.2 LTS, DRF, SimpleJWT,
# psycopg 3, pghistory (triggers PG), simple-history, django-axes, Argon2.
# =============================================================================

# --- Core framework ---
Django==5.2.1
djangorestframework==3.16.0

# --- Auth / JWT ---
djangorestframework-simplejwt==5.5.0

# --- Password hashing Argon2id (Argon2PasswordHasher primero) ---
argon2-cffi==23.1.0

# --- Lockout por fuerza bruta ---
django-axes==7.0.0

# --- Integridad append-only DB-level: triggers PostgreSQL ---
django-pghistory==3.5.0
django-pgtrigger==4.17.0

# --- PostgreSQL 17 driver (psycopg 3, binary build) ---
psycopg[binary]==3.2.6

# --- CORS para la SPA React detrás de Caddy ---
django-cors-headers==4.7.0

# --- Audit trail / version history ---
django-simple-history==3.8.0

# --- OpenAPI 3 schema + Swagger/Redoc UI ---
drf-spectacular==0.28.0

# --- WSGI server (production container) ---
gunicorn==23.0.0
